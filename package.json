{
  "name": "vertex-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Vertex AI with a local Node backend.",
  "scripts": {
    "build": "node scripts/build-static.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js"
  }
}
